{
  "name": "trustportal-extension",
  "version": "1.0.0",
  "description": "TrustPortal / TSIF Chrome Extension (MV3 + TypeScript + Closed Shadow DOM)",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.1.0"
  }
}
