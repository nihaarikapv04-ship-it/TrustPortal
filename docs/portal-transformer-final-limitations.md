# Explicit System Limitations & Non-Claims (`docs/portal-transformer-final-limitations.md`)

> **Scientific Limitations Standard**: Explicitly catalogs all unevaluated scope items and benchmark bounds.

---

## 1. Explicit Non-Claims & Unevaluated Scope Items

1. **NVDA Spoken Audio Testing**: **`NOT EVALUATED`** (Headless CLI environment limitation). Evaluated via DOM accessibility-tree semantic state tracking.
2. **TalkBack Mobile Spoken Audio Testing**: **`NOT EVALUATED`** (Headless CLI environment limitation).
3. **Human-Subject Usability Study**: **`NOT EVALUATED`** (No IRB-approved human participant trial conducted).
4. **Cloud Provider RTT Latency**: **`NOT EVALUATED`** (Evaluated via local deterministic mock provider at $0.0\text{ ms}$).
5. **Large-Scale Representative `.gov.in` Study**: **`NOT EVALUATED`** (Evaluated on a curated 20-page sample; population-level generalization is unclaimable).
6. **UPI Benchmark Scope**: UPI security results ($N=1,500$) are **`BENCHMARK-SCOPED`** to the 20 structural templates and 1,480 parameterized variants; they do not constitute independent generalization evidence.
